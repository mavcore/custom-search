/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = nextConfig

module.exports = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  images: {
    domains: ['www.google.co.uk', 'i.postimg.cc'],
  },
};

